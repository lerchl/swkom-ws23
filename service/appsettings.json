{
	"ConnectionStrings": {
		"PostgreContext": "Include Error Detail=True;Host=localhost;Database=paperless;Username=postgres;Password=postgres",
		"RabbitMQ": "host=localhost;username=rabbitmq;password=rabbitmq"
	},
    "Minio": {
        "endpoint": "localhost:9000",
        "accesskey": "minio",
        "secretkey": "minio123"
    }
}
